# 测试样例说明

`minimal_turn.json` 是根据《未来战争》v1.0 接口文档手工构造的合成观测，不是正式比赛回放。

来自规则的字段包括 41×32 地图、挑战者阵营、2×2 基地以左上角坐标表示、两个工人、一个开拓者，以及请求顶层字段。角色 ID 沿用文档公布的挑战者初始分配规则。

坐标、75 金币、奖励数值和中文世界消息仅用于测试。空敌方角色、空机器人和空商店也只是本场景设定，不能据此推断正式比赛初始状态。

样例特意省略任务的 `timeoutRounds` 和单位的 `cooldown`，验证文档样例中缺失的信息会保留为未知值，而不是被解释为零。
